{
  "name": "@codenames/domain",
  "version": "0.1.0",
  "description": "Domain of the Codenames web version",
  "author": "Paul Landázuri",
  "license": "GPL-3.0-or-later",
  "main": "src/index.ts",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "devDependencies": {
    "typescript": "^3.7.4"
  }
}
